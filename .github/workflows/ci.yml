name: CI

"on":
  push:
    branches: [main]
    tags: ["v*.*.*"]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

permissions:
  contents: write

jobs:
  ci:
    uses: mbologna/.github/.github/workflows/node-ci.yml@cb16b82ad4f9d2044480334946870c2c6a1c8d57 # main
    with:
      node_version: lts/*
      package_manager: npm
      test_command: "check"
      enable_audit: true
      enable_release_drafter: true

  auto-release:
    if: github.event_name == 'schedule'
    uses: mbologna/.github/.github/workflows/auto-release.yml@cb16b82ad4f9d2044480334946870c2c6a1c8d57 # main
    with:
      strategy: commits
    secrets: inherit
