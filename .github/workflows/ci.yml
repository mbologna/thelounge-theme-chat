name: CI

"on":
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: mbologna/.github/.github/workflows/node-ci.yml@main
    with:
      node_version: lts/*
      package_manager: npm
      test_command: ""
