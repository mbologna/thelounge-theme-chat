name: CI

"on":
  push:
    branches: [main]
    tags: ["v*.*.*"]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

permissions:
  contents: write

jobs:
  ci:
    uses: mbologna/.github/.github/workflows/node-ci.yml@64ba96e6b7e5d32b120c30baf0db6713e0f87cda # main
    with:
      node_version: lts/*
      package_manager: npm
      test_command: "check"
      enable_audit: true
      enable_release_drafter: true

  auto-release:
    if: github.event_name == 'schedule'
    uses: mbologna/.github/.github/workflows/auto-release.yml@64ba96e6b7e5d32b120c30baf0db6713e0f87cda # main
    with:
      strategy: commits
    secrets: inherit
