name: CI

"on":
  push:
    branches: [main]
    tags: ["v*.*.*"]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

permissions:
  contents: write

jobs:
  ci:
    uses: mbologna/.github/.github/workflows/node-ci.yml@da191150b8ae5eefd50d0bfa6b6b4d7a012a1dd6 # main
    with:
      node_version: lts/*
      package_manager: npm
      test_command: "check"
      enable_audit: true
      enable_release_drafter: true

  auto-release:
    if: github.event_name == 'schedule'
    uses: mbologna/.github/.github/workflows/auto-release.yml@da191150b8ae5eefd50d0bfa6b6b4d7a012a1dd6 # main
    with:
      strategy: commits
    secrets: inherit
