name: Publish to npm

"on":
  release:
    types: [published]

jobs:
  publish:
    uses: mbologna/.github/.github/workflows/npm-publish.yml@main
    with:
      node_version: lts/*
      package_manager: npm
    secrets: inherit

